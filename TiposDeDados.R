# Tipos de dados

# Inteiro - Integer
x <- 4L
class(x) # Sempre colocar o L para denotar inteiros

# Ponto flutuante - Numeric
y <- 4.3
class(y)

# Caractere - Character
z <- "Batata"
class(z)

# Logico - Logical
w <- TRUE
u <- F # Pode representar como T ou F tambem
class(w)
class(u)
