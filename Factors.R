# Factors

# Setup
paises <- c("BR","ARG","PAR","CHI")

pfactor <- factor(paises)
pfactor
str(pfactor)
summary(pfactor) # Mostra quantas pessoas tem de cada país

# Ordena por ordem alfabética,número,etc [Possui ordenação]