# Outras funções (parte 2)

tabela <- read.table(file.choose(), sep = ";", dec = ".", header = T)

# Header - Indica se a primeira linha é cabeçalho dos dados
# Sep -  Define o separador dos dados
# row.names - Define um nome para as linhas
# col.names - Define um nome para as colunas
# Dec - Define o caractere usado como separador decimal
